# Performance Benchmarks — DISABLED
# Temporarily disabled: benchmark crate removed until proper benches are implemented.
# Re-enable after production launch with proper faq_direct_hit / local_llm_routing benches.
#
# name: Performance Benchmarks
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
